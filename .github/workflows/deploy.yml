name: Build and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: docker build -t stock-web-chat .

      - name: Test backend imports
        run: |
          docker run --rm stock-web-chat uv run python -c "
          from server.app import app
          from server.config_manager import ConfigManager
          from server.agent.runner import run_agent_loop
          from server.agent.tools import _make_tools
          print('All imports OK')
          "

      # Uncomment to push to a registry:
      # - name: Login to Registry
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      #
      # - name: Push Docker image
      #   run: |
      #     docker tag stock-web-chat ghcr.io/${{ github.repository }}:latest
      #     docker push ghcr.io/${{ github.repository }}:latest
